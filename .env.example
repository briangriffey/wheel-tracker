# Market Data Configuration
# Configure the market data provider for fetching stock prices and historical data

# Provider to use (options: mock, alpha_vantage, polygon, yahoo_finance, iex_cloud)
# Default: mock (perfect for development and testing, no API key required)
MARKET_DATA_PROVIDER=mock

# API key for the selected provider (not required for mock provider)
# Get your API key from the provider's website:
# - Alpha Vantage: https://www.alphavantage.co/support/#api-key
# - Polygon.io: https://polygon.io/dashboard/api-keys
# - IEX Cloud: https://iexcloud.io/console/tokens
MARKET_DATA_API_KEY=

# Rate limit (requests per minute)
# Adjust based on your provider's plan limits
# Default: 60
MARKET_DATA_RATE_LIMIT=60

# Cache TTL (time-to-live in seconds)
# How long to cache market data responses
# Default: 300 (5 minutes)
MARKET_DATA_CACHE_TTL=300

# Retry attempts on transient failures
# Number of times to retry failed requests
# Default: 3
MARKET_DATA_RETRY_ATTEMPTS=3
